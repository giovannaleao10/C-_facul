#include<iostream>
#include<math.h>
using namespace std;

int main(void){

    int a, b, c, r;

    cin >> a;
    cin >> b;
    cin >> c;

    r = pow(a + b + c,2);

    cout << r;

    return 0;
}
