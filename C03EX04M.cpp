#include<iostream>
#include<math.h>
using namespace std;

int main(void){

    int a, b, c, r;

    cin >> a;
    cin >> b;
    cin >> c;

    r = pow(a,2) + pow(b,2) + pow(c,2);

    cout << r;

    return 0;
}
